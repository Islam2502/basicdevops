services:
  frontend:
    build:  ./frontend
    ports: 
      - '8000:8000'
    volumes:
      - ./frontend:/app

  backend:
    build: ./backend
    ports: 
      - '80:80'
    volumes:
      - /app/__pycache__
      - /app/venv
      - ./backend:/app
    
